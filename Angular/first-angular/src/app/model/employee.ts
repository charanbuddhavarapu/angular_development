export interface IEmployee {

    //In this interface we will mention the structure of the object

   id: string;
  firstName: string;
  lastName: string;
  email: string;
  gender: string;
  sal: number;
}
